# Установка библиотеки TCPDF для генерации PDF отчётов

Для работы функции экспорта в PDF необходимо установить библиотеку TCPDF.

## Способ 1: Установка через Composer (рекомендуется)

1. Убедитесь, что у вас установлен Composer: https://getcomposer.org/

2. Выполните в корне проекта:
```bash
composer install
```

Или напрямую установите TCPDF:
```bash
composer require tecnickcom/tcpdf
```

## Способ 2: Ручная установка

1. Скачайте TCPDF с GitHub: https://github.com/tecnickcom/TCPDF

2. Распакуйте архив и скопируйте папку `tcpdf` в папку `includes/` проекта

3. Структура должна быть такой:
```
DBGranit/
├── includes/
│   ├── PDFReportGenerator.php
│   └── tcpdf/
│       └── tcpdf.php
```

## Способ 3: Автоматическая установка (Windows)

1. Запустите PowerShell скрипт:
   ```powershell
   powershell -ExecutionPolicy Bypass -File install_tcpdf.ps1
   ```

2. Скрипт автоматически скачает и установит TCPDF в `includes/tcpdf/`

## Проверка установки

После установки откройте любую страницу с таблицей (например, Пользователи) и нажмите кнопку "📄 Экспорт в PDF". Если библиотека установлена правильно, откроется PDF файл с отчётом.

## Решение проблем

Если появляется ошибка "TCPDF library not found":
- Проверьте, что папка `vendor/` существует (при установке через Composer)
- Или проверьте, что папка `includes/tcpdf/` существует (при ручной установке)
- Убедитесь, что файл `tcpdf.php` находится в правильной папке
